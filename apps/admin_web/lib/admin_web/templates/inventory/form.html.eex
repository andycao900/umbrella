<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :dealer_id %>
  <%= text_input f, :dealer_id %>
  <%= error_tag f, :dealer_id %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
