defmodule Authentication do
  @moduledoc """
  Library for common authentication modules/plugs. Depends on Engine, but cannot reference other
  Web apps or circular dependencies will break the build.
  """
end
